
type Foo {
    Foo(Int)
}

main {
    b.method x => {
        print(x)
    }
    let a = () => { () }
      
    let top_students = from(students). 
        filter(x => (x.gdp).gt(90)).
        select(x => type { 
            name=x.name
            surname=x.surname
        })
}   
