// DOES NOT COMPILE
// THIS DOESN'T EVEN MAKE SENSE
// IT'S MORE OF AN IDEA BLUEPRINT

type Foo {
    Foo(Int)
}

main {
    b.method x => {
        print(x)
    }
    let a = () => { () }
      
    let top_students = from(students). 
        filter(x => (x.gdp).gt(90)).
        select(x => type { 
            name=x.name
            surname=x.surname
        })
}   
